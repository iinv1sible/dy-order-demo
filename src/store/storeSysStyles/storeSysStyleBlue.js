import utils from "@/utils/utils";
let storeSysStyleBlue = {
  namespaced: true,
  state: {
    pageIndexNavbarSearch: {
      nsSearchParentIStyle: utils.styles({
        backgroundColor: "blue"
      })
    }
  }
};

export default storeSysStyleBlue;
