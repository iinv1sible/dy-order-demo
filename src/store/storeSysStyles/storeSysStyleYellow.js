import utils from "@/utils/utils";
let storeSysStyleYellow = {
  namespaced: true,
  state: {
    pageIndexNavbarSearch: {
      nsSearchParentIStyle: utils.styles({
        backgroundColor: "yellow"
      })
    }
  }
};

export default storeSysStyleYellow;
